package engine;

public class workSheet {
	// Parameters that progress the game, some matter, some don't.
   
	public static boolean uniqueconvo1 = false; // This is will change to true, if the conversation is had, lifting the parameter.
    public static boolean uniqueconvo2 = false; // This is will change to true, if the conversation is had, lifting the parameter.
    public static boolean convo3 = false; // This conversation is different because it's not unique, 
    										// meaning it doesn't prevent the story from progressing
    
    public static boolean uniqueitem1 = false; // This will change to true once the item is picked up.
    public static boolean uniqueitem2 = false; // This will change to true once the item is picked up.
    public static boolean item3 = false; // This will change to true once the item is picked up.
    public static boolean uniquevidence1 = false; // This will change to true once the item is discovered.
    public static boolean uniquevidence2 = false; // This will change to true once the item is discovered.
    public static boolean evidence3 = false; // This will change to true once the item is discovered.
    public static boolean puzzle1 = false; // This will change to true once the item is discovered.
    
    public static void main(String[] args) {
        // You can modify test1 here if needed
    }

    public static void partOne() {
        // You can modify test1 here if needed
    }
}